# M03 开工清单（应用模块与部署）

## 1. 分支与流程检查
- [x] 当前分支为临时分支（非 `master`）
- [x] 分支命名符合：`temp/m03-yyyymmdd-主题`
- [x] `REFACTOR_PLAN.md` 与 `CHANGELOGS.md` 已切换到 M03 状态

## 2. M03 对标范围（对标第 5 期）
- [x] 对标提交 `868c65b`：应用模块（生成 CRUD 基础代码）
  - [x] 应用实体 / Schema / Service / API 基础结构补齐
  - [x] 应用 CRUD 基础接口打通
- [x] 对标提交 `9112c1f`：应用模块（完成基本功能）
  - [x] 应用详情与分页查询能力完善
  - [x] 鉴权与数据归属校验补齐（本人/管理员）
- [x] 对标提交 `e05b173`：应用模块（小优化）
  - [x] 接口参数校验与错误处理优化
  - [x] 应用模块代码结构与复用优化
- [x] 对标提交 `44f81d6`：应用模块（应用生成）
  - [x] 应用生成链路与应用管理流程打通
  - [x] 生成产物与应用记录建立关联
- [x] 对标提交 `fc48d1e`：应用模块（应用部署）
  - [x] 应用部署能力（deploy key / deployed time）落库
  - [x] 静态访问与下载链路打通
- [x] 对标提交 `81eeda3`：应用模块（前端）
  - [x] 前端应用管理页面与接口对齐
  - [x] 前后端联调通过

## 3. 计划落地顺序
1. 先完成应用 CRUD 与分页（后端）
2. 再完成部署、静态访问、下载链路（后端）
3. 最后完成前端适配与端到端联调

## 4. M03 DoD（验收门禁）
- [x] 应用 CRUD 可用（新增 / 查询 / 更新 / 删除 / 分页）
- [x] 应用部署能力可用（含部署状态字段）
- [x] 静态访问链路可用（可访问已部署内容）
- [x] 下载链路可用（可导出应用产物）
- [x] 前后端主流程可完整演示
- [x] `uv run pytest -q -p no:faulthandler` 通过
- [x] `npm run build` 通过
- [x] `CHANGELOGS.md` 已更新
- [x] 提交信息使用中文

## 5. 推荐提交拆分（中文）
1. `feat(m03): 完成应用CRUD与分页接口`
2. `feat(m03): 增加应用部署与静态访问能力`
3. `feat(m03): 增加应用下载链路`
4. `feat(m03): 完成前端应用模块适配`
5. `test(m03): 增加应用模块集成测试`
6. `docs(m03): 更新验收清单与阶段日志`
