# M02 开工清单（AI 应用生成初版）

## 1. 分支与流程检查
- [x] 当前分支为临时分支（非 `master`）
- [x] 分支命名符合：`temp/m02-yyyymmdd-主题`
- [x] `REFACTOR_PLAN.md` 与 `CHANGELOGS.md` 已切换到 M02 状态

## 2. M02 对标范围（对标第 4 期）
- [x] 对标提交 `2f783b3`：打通 AI 应用生成主链路
  - [x] AI 对话服务接入（Python 版）
  - [x] Prompt 资源加载（HTML / 多文件）
  - [x] 代码解析器（HTML / 多文件）
  - [x] 文件保存器（生成结果落盘）
  - [x] 生成门面（统一编排入口）
  - [x] SSE 流式输出与解析
- [x] 对标提交 `6c38f1d`：完成第一轮结构优化
  - [x] 解析器策略化（解析器接口 + 执行器）
  - [x] 保存器模板化（模板 + 执行器）
  - [x] 抽象通用 SSE 流处理方法

## 3. 计划落地顺序
1. 先完成最小可用链路（请求 -> AI -> 解析 -> 保存 -> 响应）
2. 再补 SSE 流式输出链路
3. 最后做策略/模板执行器重构，保证可扩展

## 4. M02 DoD（验收门禁）
- [x] 至少 1 个 AI 生成接口可用（非 mock）
- [x] 至少支持 1 种代码生成类型（建议先 HTML）
- [x] 生成文件可成功落盘并可读取
- [x] SSE 流式输出可正常消费
- [x] `uv run pytest -q` 通过
- [x] `npm run build` 通过
- [x] `CHANGELOGS.md` 已更新
- [x] 提交信息使用中文

## 5. 推荐提交拆分（中文）
1. `feat(m02): 接入AI生成最小链路与Prompt加载`
2. `feat(m02): 实现代码解析与文件保存基础能力`
3. `feat(m02): 增加SSE流式输出与消费处理`
4. `refactor(m02): 解析器与保存器重构为执行器模式`
5. `test(m02): 增加AI生成链路集成测试`
6. `docs(m02): 更新验收记录与阶段日志`
