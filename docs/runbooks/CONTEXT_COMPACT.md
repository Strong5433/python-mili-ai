# 会话压缩摘要（M00 完成后）

## 当前基线
- 主分支：`master`
- 最新合并：`202d2ce feat(m00): 完成初始化与基础依赖阶段`
- 工作区：干净

## 已完成（M00）
- 后端单体骨架：FastAPI + 统一响应 + 错误码 + 全局异常
- 基础能力：CORS、请求日志中间件、资源管理器（SQLAlchemy/Redis 骨架）
- 数据迁移：Alembic 骨架可执行
- 前端接入：保留原技术栈，`/api` 代理至 `8123`，构建通过
- 验收：`uv run pytest -q` 通过、`npm run build` 通过

## 强制流程
- 所有开发仅在临时分支
- 验收通过后才允许合并 `master`
- 提交信息中文
- `CHANGELOGS.md` 中文维护

## 下一阶段
- 进入 M01（用户模块）：注册、登录、登录态、退出、基础权限

